% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/culex_dedup.R
\name{culex_dedup}
\alias{culex_dedup}
\title{Resolve within-week collection-date near-duplicates}
\usage{
culex_dedup(df, key_cols = c("trap_id", "year", "week"), verbose = TRUE)
}
\arguments{
\item{df}{Data frame containing \code{key_cols} plus \code{trap_date} and \code{total}.}

\item{key_cols}{Character vector identifying a trap-week. Passed to
\code{\link[=make_key]{make_key()}} — must have at least two elements. Default
\code{c("trap_id", "year", "week")}.}

\item{verbose}{Logical. If \code{TRUE} (default), summary cli alerts are
printed describing what was removed.}
}
\value{
Deduplicated data frame.
}
\description{
Some VDCI weekly files contain two collection events for the same trap
within the same ISO week — one weekday (routine surveillance) and one
weekend (field test or aberrant run). They share the same
\verb{(trap_id, spp, year, week)} key but differ in \code{trap_date}, so generic
\code{distinct()} will not remove them. Downstream this creates duplicate keys
when joining against the wnv-s_database (which aggregates to one row per
trap-week).
}
\details{
Deduplication is conservative:
\itemize{
\item Single-date trap-weeks pass through unchanged (lone weekend
collections are never removed).
\item Multi-date trap-weeks that include a weekend: keep the collection date
with the highest total mosquito count summed across all species.
Ties favour weekday dates, then the later date. Exactly one row per
near-duplicate trap-week survives.
}

Must be called \emph{after} \code{\link[=wnv_s_clean]{wnv_s_clean()}} so that \code{trap_date} is class \code{Date}
and \code{year} / \code{week} have been derived from it.
}
