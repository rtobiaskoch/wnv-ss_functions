% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_abund.R
\name{calc_abund}
\alias{calc_abund}
\title{Calculate mean mosquito abundance per light trap}
\usage{
calc_abund(
  df,
  grp_var = c("zone", "year", "week", "spp"),
  spp_keep = wnvSurv::spp_levels,
  rm_zone = NULL
)
}
\arguments{
\item{df}{Cleaned culex datasheet. Must contain \code{trap_id}, \code{year}, \code{week},
\code{zone}, \code{zone2}, \code{trap_status}, \code{method}, \code{spp}, \code{total}.}

\item{grp_var}{Character vector of columns to group by before summarising.
Default \code{c("zone", "year", "week", "spp")}. Use \code{"zone2"} instead of
\code{"zone"} to roll FC quadrants up to the composite group.}

\item{spp_keep}{Species to include. Default \code{wnvSurv::spp_levels}
(\code{c("Tarsalis", "Pipiens")}).}

\item{rm_zone}{Zones to drop entirely before computing. Default \code{NULL}.}
}
\value{
A tibble with one row per group containing \code{trap_L} (number of
distinct light traps), \code{mosq_L} (sum of \code{total}), \code{abund} (mean per
trap), \code{abund_sd}, and \code{abund_lci} / \code{abund_uci}. If \code{zone2} was the
grouping variable it is renamed back to \code{zone}. A \code{spp = "All"} column
is added when species was not in the grouping.
}
\description{
Computes mean count per trap-night (\code{abund = mosq_L / trap_L}) with a 95\%
normal-approximation confidence interval, grouped by \code{grp_var}. Filters to
light-trap collections (\code{method == "L"}), excludes malfunction rows, and
restricts species to \code{spp_keep} before summarising. The LCI is clamped at 0
(a small-sample normal-approx CI can otherwise extend below zero).
}
\details{
Input is the cleaned all-species culex datasheet from \code{\link[=wnv_s_clean]{wnv_s_clean()}} /
\code{\link[=culex_dedup]{culex_dedup()}} — not the pooled-PCR datasheet used by \code{\link[=calc_pir]{calc_pir()}}.
}
