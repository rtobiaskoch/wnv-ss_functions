% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_vi.R
\name{calc_vi}
\alias{calc_vi}
\title{Calculate the Vector Index (VI = abundance × PIR)}
\usage{
calc_vi(
  abund,
  pir,
  by = c("year", "week", "zone", "spp"),
  spp_cmplt = wnvSurv::spp_levels,
  zone_complete = wnvSurv::zone_lvls,
  complete = FALSE,
  rm_zone = NULL
)
}
\arguments{
\item{abund}{Abundance tibble from \code{\link[=calc_abund]{calc_abund()}}.}

\item{pir}{PIR tibble from \code{\link[=calc_pir]{calc_pir()}}.}

\item{by}{Character vector of join keys. Default
\code{c("year", "week", "zone", "spp")}.}

\item{spp_cmplt}{Species to include when \code{complete = TRUE}. Default
\code{wnvSurv::spp_levels}.}

\item{zone_complete}{Zones to include when \code{complete = TRUE}. Default
\code{wnvSurv::zone_lvls}. Replaces the undefined \code{grp_zones} global in the
upstream version.}

\item{complete}{Logical. If \code{TRUE}, fill the output to a full zone ×
species × year × week grid via \code{\link[tidyr:complete]{tidyr::complete()}}. Default \code{FALSE}
(avoids the upstream \code{grp_zones} crash).}

\item{rm_zone}{Zones to drop entirely. Default \code{NULL}.}
}
\value{
A tibble with \code{vi}, \code{vi_lci}, \code{vi_uci} columns plus the join keys,
sorted by \verb{year, week, zone, spp}.
}
\description{
Joins abundance and PIR estimates on \code{by}, then computes
\code{vi = abund * pir} (and matching CI from \code{abund * pir_lci/pir_uci}). VI is
the standard summary metric of local WNV risk: it is the expected number
of infected mosquitoes caught per trap-night. Optionally completes the
output to a full zone × species × year × week grid.
}
