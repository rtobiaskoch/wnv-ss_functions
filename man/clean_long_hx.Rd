% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hx.R
\name{clean_long_hx}
\alias{clean_long_hx}
\title{Build the long-format current-vs-historical dataframe (yearly lines)}
\usage{
clean_long_hx(
  ytd,
  hx,
  rm_zone = "BC",
  grp_var = c("year", "week", "zone", "spp", "type"),
  est_keep = c("abund", "vi", "pir"),
  spp_keep = c("All")
)
}
\arguments{
\item{ytd}{Current year-to-date estimates.}

\item{hx}{Historical estimates.}

\item{rm_zone}{Zones to drop. Default \code{"BC"}.}

\item{grp_var}{Columns to keep alongside the estimate columns. Default
\code{c("year", "week", "zone", "spp", "type")}.}

\item{est_keep}{Estimate names to retain. Default \code{c("abund", "vi", "pir")}.}

\item{spp_keep}{Species to retain. Default \code{c("All")}.}
}
\value{
A long-format tibble.
}
\description{
Binds and reshapes estimates into a long format suitable for
\code{\link[=plot_hx_line]{plot_hx_line()}} — one row per zone × week × spp × year × type ×
estimate. Drops \code{rm_zone} and restricts to species in \code{spp_keep}.
}
\keyword{internal}
