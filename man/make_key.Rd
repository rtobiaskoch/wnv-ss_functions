% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_key.R
\name{make_key}
\alias{make_key}
\title{Build a composite key column from multiple columns}
\usage{
make_key(df, key_cols, name = "key", sep = "|")
}
\arguments{
\item{df}{A data frame.}

\item{key_cols}{Character vector of column names to concatenate. At least
two names must be supplied.}

\item{name}{Name of the new key column. Default \code{"key"}.}

\item{sep}{Separator string inserted between values. Default \code{"|"}.
Non-alphanumeric separators are preserved verbatim (only the values are
sanitised, not the separator).}
}
\value{
The input data frame with one additional column (named \code{name})
placed first.
}
\description{
Concatenates the values of \code{key_cols} (after stripping non-alphanumeric
characters from each value) into a single string column joined by \code{sep}.
Originals are untouched — only the key is added. Used to create stable
join keys across heterogeneous data sources where small whitespace or
punctuation differences would otherwise prevent matching.
}
\examples{
\dontrun{
make_key(culex, c("trap_id", "spp", "year", "week"))
}

}
