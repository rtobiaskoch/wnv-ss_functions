% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key_rename.R
\name{key_rename}
\alias{key_rename}
\title{Rename data frame columns via a lookup table}
\usage{
key_rename(df, rename_df, drop_extra = FALSE)
}
\arguments{
\item{df}{A data frame to rename columns of.}

\item{rename_df}{A data frame with two character columns: \code{old} (current
column names in \code{df}) and \code{new} (desired names).}

\item{drop_extra}{Logical. If \code{TRUE}, columns in \code{df} that have no entry in
\code{rename_df$old} are dropped from the output; if \code{FALSE} (the default),
they are passed through with their original names.}
}
\value{
A data frame with renamed (and optionally pruned) columns.
}
\description{
Renames columns in \code{df} according to a two-column lookup data frame (\code{old}
→ \code{new}). Unmatched columns are left untouched by default, or dropped if
\code{drop_extra = TRUE}. Lookup rows that reference columns not present in \code{df}
are silently ignored, so a single canonical rename map can be reused across
heterogeneous source files.
}
\details{
Matching is \strong{whitespace-insensitive}: both the \code{df} column names and the
lookup's \code{old} values are collapsed (internal runs of whitespace reduced to a
single space and trimmed) before comparison. This is deliberate — source
spreadsheets routinely carry cosmetic spacing that the lookup cannot
anticipate (e.g. a template header \code{"Collection Site       (Trap ID)"} with
seven spaces vs a one-space lookup entry). The \strong{actual} column name in \code{df}
is what gets renamed, so no data is altered. If the lookup lists the same
column under several whitespace/alias variants, the first is used. Renaming
that would collapse two different columns onto one name is an error, not a
silent drop.
}
\examples{
\dontrun{
rename_map <- tibble::tribble(
  ~old,             ~new,
  "Trap ID",        "trap_id",
  "Collection Date", "trap_date"
)
key_rename(raw_culex, rename_map)
}

}
