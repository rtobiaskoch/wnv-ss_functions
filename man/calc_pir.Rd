% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_pir.R
\name{calc_pir}
\alias{calc_pir}
\title{Calculate Pooled Infection Rate (PIR) via the Firth MLE}
\usage{
calc_pir(
  df,
  grp_var = c("zone", "year", "week", "spp"),
  zone_complete = wnvSurv::zone_lvls,
  rm_zone = NULL
)
}
\arguments{
\item{df}{Pool-level datasheet. Must contain \code{trap_id}, \code{year}, \code{week},
\code{zone}, \code{zone2}, \code{method}, \code{spp}, \code{total}, \code{test_code}.}

\item{grp_var}{Character vector of columns to group by. Default
\code{c("zone", "year", "week", "spp")}. Cannot contain \code{"csu_id"}.}

\item{zone_complete}{Character vector of zones to complete in the output
grid. Default \code{wnvSurv::zone_lvls}. \code{rm_zone} is subtracted from this.}

\item{rm_zone}{Zones to drop entirely. Default \code{NULL}.}
}
\value{
A tibble with one row per \code{grp_var} combination containing \code{pir},
\code{pir_lci}, \code{pir_uci}. Renames \code{zone2} to \code{zone} if present and adds
\code{spp = "All"} when species was not in the grouping.
}
\description{
Estimates the bias-corrected maximum-likelihood pooled infection rate
(infected mosquitoes per pool member) for each \code{grp_var} combination, using
\code{\link[PooledInfRate:pooledBin]{PooledInfRate::pIR()}} with \code{pt.method = "firth"}. Pools of size 1 — which
break the MLE — are handled via a separate edge-case branch that assigns
PIR = \code{test_code} (0 or 1) for those single-mosquito pools. Missing
group/zone combinations are completed with PIR = 0.
}
\details{
Input is the pooled (PCR-tested) datasheet, \strong{not} the all-species culex
trap datasheet used by \code{\link[=calc_abund]{calc_abund()}}. Required columns include
\code{test_code} (0 = negative, 1 = positive).
}
