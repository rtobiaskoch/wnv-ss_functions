% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wnv_s_clean.R
\name{wnv_s_clean}
\alias{wnv_s_clean}
\title{Clean a Culex surveillance data sheet}
\usage{
wnv_s_clean(
  df,
  all_cols = c("csu_id", "trap_id", "zone", "zone2", "trap_date", "year", "week", "spp",
    "spp0", "method", "trap_status", "total"),
  rm_col = c(),
  verbose = TRUE
)
}
\arguments{
\item{df}{A data frame with raw Culex surveillance data. Expected columns
are a subset of \code{all_cols}.}

\item{all_cols}{Character vector of canonical column names that this
function knows how to clean and that it will pull to the front of the
output. Defaults match the wnv-s_database schema.}

\item{rm_col}{Character vector of column names to \emph{skip} cleaning for
(still kept in the data, just not transformed).}

\item{verbose}{Logical. If \code{TRUE} (default), per-step cli alerts are
printed. Set \code{FALSE} in pipelines / tests for silent operation.}
}
\value{
A cleaned data frame, sorted by \code{trap_date} (desc) and \code{trap_id}
when those columns are present.
}
\description{
Standardises raw Culex mosquito surveillance data: trims whitespace, parses
collection dates with \code{\link[=parse_flexible_date]{parse_flexible_date()}}, normalises zone and \code{csu_id}
values, derives \code{year}/\code{week} from \code{trap_date}, canonicalises trap IDs and
species labels, and computes a per-trap-night \code{trap_status} (\code{culex} /
\verb{no culex} / \verb{no mosquitoes} / \code{malfunction} / \verb{no trap}). Each step runs
only if its inputs are present, so the function works on partial frames.
}
\details{
Must be paired with \code{\link[=key_rename]{key_rename()}} beforehand if the input column names do
not yet match the canonical schema (\code{trap_id}, \code{trap_date}, \code{spp}, \code{total},
…). Run \code{\link[=culex_dedup]{culex_dedup()}} afterward to resolve within-week duplicates.
}
\examples{
\dontrun{
raw_culex |>
  key_rename(rename_map) |>
  wnv_s_clean(verbose = FALSE) |>
  culex_dedup()
}

}
