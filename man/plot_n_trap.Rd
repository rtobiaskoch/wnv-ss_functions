% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_n_trap.R
\name{plot_n_trap}
\alias{plot_n_trap}
\title{Bar chart of trap counts per week, by status, faceted by zone × year}
\usage{
plot_n_trap(
  df,
  rm_zone = NULL,
  year_start = NULL,
  year_end = NULL,
  week_breaks = seq(23, 37, by = 5),
  week_limits = c(23, 37)
)
}
\arguments{
\item{df}{A cleaned culex datasheet with at least \code{trap_id}, \code{year},
\code{week}, \code{zone}, \code{zone2}, and \code{trap_status}. Run \code{\link[=wnv_s_clean]{wnv_s_clean()}} first.}

\item{rm_zone}{Zones to drop entirely before plotting. Default \code{NULL}.}

\item{year_start}{Earliest year to include (inclusive). Default \code{NULL}
(no lower bound).}

\item{year_end}{Latest year to include (inclusive). Default \code{NULL}
(no upper bound).}

\item{week_breaks}{Numeric vector of x-axis breaks. Default \code{seq(23, 37, by = 5)} (the canonical surveillance season).}

\item{week_limits}{Numeric length-2 vector of x-axis limits. Default
\code{c(23, 37)}.}
}
\value{
A \code{ggplot} object.
}
\description{
Collapses duplicate \verb{(trap_id, year, week)} rows from joined sources to a
single representative \code{trap_status} using the priority
\strong{malfunction > culex > no culex > no mosquitoes}, then counts distinct
trap IDs per zone × week and plots as a stacked column chart faceted by
\code{zone2 ~ year}. Colours come from \link{trap_status_colors}.
}
