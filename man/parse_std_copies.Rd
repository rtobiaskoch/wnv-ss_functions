% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_curve.R
\name{parse_std_copies}
\alias{parse_std_copies}
\title{Parse the known copy number from a qPCR standard label}
\usage{
parse_std_copies(label)
}
\arguments{
\item{label}{Character vector of sample labels, e.g. \code{"wnv_std_1e6"},
\code{"std 1e2"}, \code{"1e4"}. Matching is case-insensitive.}
}
\value{
A numeric vector of starting copy numbers; \code{NA_real_} for labels that
carry no \verb{1e<digits>} token (non-standards such as \code{"mozzy"}, \code{"neg ctrl"}).
}
\description{
Standards are labelled by their starting template concentration in scientific
shorthand (\code{1e2}, \code{1e4}, \code{1e6}). This pulls that number out of a free-form
label so the \emph{known} copies can be recovered from the platemap alone — i.e.
independently of whether QuantStudio's \code{Quantity} export was populated.
}
\examples{
parse_std_copies(c("wnv_std_1e6", "std 1e2", "mozzy"))  # 1e6, 1e2, NA

}
