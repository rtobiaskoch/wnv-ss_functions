% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_join.R
\name{update_join}
\alias{update_join}
\title{Update one table with values from another via a join}
\usage{
update_join(new, old, by, col_keep = names(old), arrange_desc = NULL)
}
\arguments{
\item{new}{Data frame whose values take precedence on conflict.}

\item{old}{Data frame supplying historical/fallback values and any rows not
present in \code{new}.}

\item{by}{Character vector of column names to join on.}

\item{col_keep}{Character vector of output column names/order. Default
\code{names(old)}.}

\item{arrange_desc}{Optional column name to sort the result by, descending.
Default \code{NULL} (no sorting).}
}
\value{
A data frame with one row per distinct \code{by} combination present in
either input, \code{new} values preferred on conflict, columns ordered as
\code{col_keep}.
}
\description{
Full-joins \code{new} and \code{old} on \code{by}, then coalesces every shared non-key
column so that \code{new} values win where both sides have data and \code{old}
values fill in everywhere else. Generic, composable join for "update table
x with the latest data from table y" — e.g. merging this week's trap counts
into a historical trap database, keyed on a stable composite key.
}
\details{
\code{dplyr::coalesce()} errors when combining a factor column with a character
column, which happens when one side has been through \code{wnv_s_clean()}
(factors) and the other hasn't. Factor columns in both \code{new} and \code{old} are
coerced to character before joining; \code{wnv_s_clean()} re-factors downstream
as needed. \code{dplyr::full_join()} (unlike \code{rquery::natural_join()}) preserves
factor levels and \code{Date} classes, so no post-join type recovery is needed.
}
\examples{
\dontrun{
update_join(culex_new, culex_database, by = "key",
            col_keep = names(culex_database), arrange_desc = "trap_date")
}

}
