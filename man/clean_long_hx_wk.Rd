% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hx.R
\name{clean_long_hx_wk}
\alias{clean_long_hx_wk}
\title{Build the long-format current-vs-historical dataframe (weekly, wide-summarise)}
\usage{
clean_long_hx_wk(
  ytd,
  hx,
  rm_zone = NULL,
  grp_vars = c("year", "week", "zone", "spp")
)
}
\arguments{
\item{ytd}{Current year-to-date estimates (from \code{\link[=calc_vi]{calc_vi()}}).}

\item{hx}{Historical multi-year estimates with the same schema as \code{ytd},
plus a \code{type} column distinguishing them.}

\item{rm_zone}{Zones to drop. Default \code{NULL}.}

\item{grp_vars}{Grouping columns to pivot around. Default
\code{c("year", "week", "zone", "spp")}.}
}
\value{
A tibble with one row per zone × week × spp × type.
}
\description{
Binds current-year and historical estimates, drops requested zones,
pivots long across estimate columns, then pivots back wide on estimate
name and finally summarises the historical strata to a single mean per
\verb{(zone, week, spp)}. The output is one row per zone × week × spp × type
(\code{hx} / \code{current}) with \code{abund}, \code{pir}, and \code{vi} columns. Designed to feed
the season-overlay layout used by \code{\link[=plot_hx]{plot_hx()}}.
}
\keyword{internal}
