% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_flexible_date.R
\name{parse_flexible_date}
\alias{parse_flexible_date}
\title{Parse a single date string from any of several common formats}
\usage{
parse_flexible_date(date_str)
}
\arguments{
\item{date_str}{A single character string (length 1), or a \code{Date}, or \code{NA}.
Vectors longer than 1 raise an error — vectorise with \code{purrr::map_chr()}
at the call site (this is how \code{\link[=wnv_s_clean]{wnv_s_clean()}} uses it).}
}
\value{
A length-1 object of class \code{Date}. \code{NA_Date_}-equivalent if
parsing fails.
}
\description{
Tries a list of date formats in order and returns the first that yields a
valid \code{Date}. ISO \code{YYYY-MM-DD} is checked first, then 4-digit-year formats
(only when the input actually contains a 4-digit sequence — avoids
\verb{\%Y} greedily matching 2-digit years), then 2-digit-year formats as a
fallback. Returns \code{NA} of class \code{Date} if no format succeeds.
}
\examples{
parse_flexible_date("2024-07-15")
parse_flexible_date("7/15/24")
parse_flexible_date("15 July 2024")

}
