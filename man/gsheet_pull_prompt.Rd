% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheet_pull_prompt.R
\name{gsheet_pull_prompt}
\alias{gsheet_pull_prompt}
\title{Interactive wrapper around \code{\link[=gsheet_pull]{gsheet_pull()}}}
\usage{
gsheet_pull_prompt(
  filename,
  key,
  sheet = "Sheet1",
  force_update = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{filename}{Path to the local CSV to (over)write. Must end in \code{.csv}.}

\item{key}{The Google Sheet ID (the long string from the sheet's URL),
passed to \code{googlesheets4::read_sheet()}.}

\item{sheet}{Name of the worksheet/tab within the spreadsheet. Default
\code{"Sheet1"}.}

\item{force_update}{Logical. In non-interactive sessions, overwrite an
existing \code{filename} only if this is \code{TRUE}. In interactive sessions,
bypasses the prompt and forces the download. Default \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE} (default), emit cli alerts on each step.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
Thin wrapper that adds an interactive overwrite prompt. If \code{filename}
already exists and the R session is interactive, asks the user whether to
overwrite. In non-interactive sessions the function falls back to
\code{force_update}. The actual download is delegated to \code{\link[=gsheet_pull]{gsheet_pull()}} so the
two functions stay in lock-step.
}
\examples{
\dontrun{
gsheet_pull_prompt(
  filename = "1_input/foco_trap - data.csv",
  key      = "1AbCdEf...",
  sheet    = "data"
)
}

}
\seealso{
\code{\link[=gsheet_pull]{gsheet_pull()}} for the non-interactive core (use in pipelines).
}
