% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pcr.R
\name{plot_pcr}
\alias{plot_pcr}
\title{Plot qPCR copy-number distribution for one virus target}
\usage{
plot_pcr(
  data,
  virus,
  copy_threshold,
  week_filter,
  pattern_2_keep = "WNV|CSU|RMRP|CDC|pos|neg"
)
}
\arguments{
\item{data}{qPCR result table with columns \code{target_name}, \code{csu_id},
\code{sample_type}, \code{cq}, \code{copies}, \code{test_code}, \code{amp_status}.}

\item{virus}{Target virus, e.g. \code{"WNV"}. Matched against \code{target_name}.}

\item{copy_threshold}{Numeric detection threshold in copies; plotted as a
horizontal dashed line at \code{log(copy_threshold)}.}

\item{week_filter}{Week label used in the plot title (does not filter
data — pre-filter \code{data} if needed).}

\item{pattern_2_keep}{Regex of \code{csu_id} prefixes to retain. Default
matches WNV samples and standard controls.}
}
\value{
A \code{ggplot} object.
}
\description{
Filters the qPCR result table to a single \code{virus} (matched against
\code{target_name}) and to wells with \code{csu_id} matching \code{pattern_2_keep}
(defaults to recognised sample-type prefixes), then plots jittered
\code{log(copies)} by \code{sample_type}, coloured by \code{test_code} and shaped by
\code{amp_status}. A red dashed line marks \code{log(copy_threshold)} — the
detection limit.
}
