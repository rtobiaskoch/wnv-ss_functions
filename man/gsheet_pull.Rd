% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheet_pull.R
\name{gsheet_pull}
\alias{gsheet_pull}
\title{Download a Google Sheet to a local CSV (non-interactive core)}
\usage{
gsheet_pull(filename, key, sheet = "Sheet1", force = FALSE, verbose = TRUE)
}
\arguments{
\item{filename}{Path to the local CSV to (over)write. Must end in \code{.csv}.}

\item{key}{The Google Sheet ID (the long string from the sheet's URL),
passed to \code{googlesheets4::read_sheet()}.}

\item{sheet}{Name of the worksheet/tab within the spreadsheet. Default
\code{"Sheet1"}.}

\item{force}{Logical. If \code{TRUE}, overwrite \code{filename} even if it already
exists. Default \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE} (default), emit cli alerts on each step.}
}
\value{
Invisibly \code{TRUE}. Called for the side effect of writing
\code{filename}.
}
\description{
Authenticates against Google Drive if needed, reads the specified Google
Sheet, and writes it to a local CSV. Pure with respect to interactivity:
contains \strong{no \code{readline()} calls}, so it is safe to use inside \code{{targets}}
pipelines, Quarto renders, and CI jobs. For an interactive
"do-you-want-to-overwrite?" prompt, use \code{\link[=gsheet_pull_prompt]{gsheet_pull_prompt()}} which wraps
this function.
}
\details{
Behaviour:
\itemize{
\item If \code{filename} does not exist, it is downloaded.
\item If \code{filename} exists and \code{force = FALSE} (default), the download is
\strong{skipped} (no overwrite).
\item If \code{filename} exists and \code{force = TRUE}, it is re-downloaded and
overwritten.
}

List-columns produced by \code{googlesheets4::read_sheet()} (e.g. cells that
contain mixed types) are flattened to comma-joined character strings
before writing.
}
\examples{
\dontrun{
gsheet_pull(
  filename = "1_input/foco_trap - data.csv",
  key      = "1AbCdEf...",   # sheet ID
  sheet    = "data"
)
}

}
\seealso{
\code{\link[=gsheet_pull_prompt]{gsheet_pull_prompt()}} for the interactive variant.
}
