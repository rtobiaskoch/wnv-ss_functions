% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_platemap.R
\name{clean_platemap}
\alias{clean_platemap}
\title{Clean a PCR plate map}
\usage{
clean_platemap(
  df,
  y_pattern = "(?<=y)\\\\d+",
  w_pattern = "(?<=w)\\\\d+",
  p_pattern = "(?<=p)\\\\d+"
)
}
\arguments{
\item{df}{A data frame with at least the columns \code{csu_id}, \code{file_name},
\code{row}, and \code{column}.}

\item{y_pattern}{Lookbehind regex for the 4-digit year embedded in
\code{file_name}. Default matches the substring after \code{"y"} in e.g.
\code{"y2025_w28_p1"}.}

\item{w_pattern}{Lookbehind regex for the week number.}

\item{p_pattern}{Lookbehind regex for the plate number.}
}
\value{
A data frame with one row per non-missing well and columns
\code{well_position}, \code{csu_id}, \code{sample_type}, \code{year}, \code{week}, \code{plate}.
}
\description{
Parses plate metadata from \code{file_name} (year / week / plate number), builds
\code{well_position} from \code{row} + \code{column}, normalises \code{csu_id}, classifies each
well into a \code{sample_type} (mosquito sample, neg/pos control, standard
dilution, or bird tissue), and drops wells with no \code{csu_id}. Used in the
weekly PCR ingest pipeline.
}
